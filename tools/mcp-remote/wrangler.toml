name = "jm-ai-mcp"
main = "src/index.js"
compatibility_date = "2024-09-25"

# After first deploy, set secrets:
#   npx wrangler secret put GITHUB_TOKEN
#   npx wrangler secret put OPENAI_API_KEY
#
# Or pass them per-request via X-GitHub-Token / X-OpenAI-Key headers
# (this is what the admin panel and Android app do).
