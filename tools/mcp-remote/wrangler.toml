name = "jm-ai-mcp"
main = "src/index.js"
compatibility_date = "2024-09-25"

# After first deploy, set the shared worker auth secret:
#   npx wrangler secret put WORKER_SHARED_SECRET
#
# GitHub and OpenAI credentials must be passed per-request via
# X-GitHub-Token / X-OpenAI-Key headers. This is what the admin
# panel, VS Code client, and Android app should do.
