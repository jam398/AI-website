{
  "name": "jm-ai-mcp-server",
  "version": "1.0.0",
  "description": "MCP tool server for JM AI Consulting — Cloudflare Worker",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
